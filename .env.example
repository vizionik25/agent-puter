########################################################################################
########################## Puter.js & LiteLLM configuration ############################
########################################################################################
PUTER_AUTH_TOKEN=YOUR_PUTER_AUTH_TOKEN
PUTER_API_BASE=https://api.puter.com/puterai/openai/v1
PUTER_MODEL=openai/claude-sonnet-4-5

########################################################################################
############################## Stripe configuration ####################################
########################################################################################
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

########################################################################################
######################### API URL FOR FROM BACKEND #####################################
########################################################################################
# Local dev:             http://localhost:9999
# docker-compose.yml:    http://backend:9999  (service name on shared bridge network)
# Separate deployments:  https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:9999
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY

########################################################################################
################################### Storage ############################################
########################################################################################
# STORAGE_BACKEND options:
#   memory    (default) — in-memory, resets on restart
#   json_file           — persists to STORAGE_PATH (default: ./data/store.json)
#   puter_kv            — Puter cloud KV store (uses PUTER_AUTH_TOKEN above)
STORAGE_BACKEND=json_file
STORAGE_PATH=./data/store.json
# For puter_kv backend — override the KV API base if needed:
# PUTER_KV_BASE=https://api.puter.com/kv

########################################################################################
################################# Email Notifications ##################################
########################################################################################
# Leave blank to disable email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your-app-password
FROM_EMAIL=noreply@yourdomain.com
FRONTEND_URL=http://localhost:3000

########################################################################################
################################# Multi-tenancy ########################################
########################################################################################
# Comma-separated key:tenant pairs. Requests with X-Agency-Key header are routed
# to the matching tenant namespace. Requests without a key land in "default".
# Example: AGENCY_API_KEYS=secretkey1:acme,secretkey2:contoso
AGENCY_API_KEYS=

########################################################################################
################################### Admin API ##########################################
########################################################################################
# Protects all /api/admin/* routes. Set to a strong secret.
ADMIN_API_KEY=change-me-in-production

########################################################################################
################################## MCP Integration #####################################
########################################################################################
# When set, the Researcher and Engineer agents connect to this MCP server
# and expose its tools with the "mcp_" prefix.
# Example: MCP_SERVER_URL=http://localhost:3100/mcp
MCP_SERVER_URL=
